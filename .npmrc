# @capacitor/cli v8 exige Node >=22 (y no hay ninguna versión de la v8 que
# acepte menos). Sin engine-strict, npm instala igual y el error aparece
# recién al correr `cap sync`, con un mensaje que no dice qué hacer:
#   [fatal] The Capacitor CLI requires NodeJS >=22.0.0
engine-strict=true
